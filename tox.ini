[tox]
envlist = py37,py38,py39,py310linting

# [testenv]
# commands = pytest {posargs:tests}

# [testenv:linting]
# skip_install = True
# deps = pre-commit
# commands = pre-commit run --all-files --show-diff-on-failure
